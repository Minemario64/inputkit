[project]
name = "inputkit"
version = "0.1.0"
description = "A cross-platform package for getting and handling raw input"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "tester",
]

[tool.uv.sources]
tester = { git = "https://github.com/Minemario64/Testing-Suite.git" }
